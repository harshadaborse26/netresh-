
pipeline {
    agent any

    stages {

        stage('PULL') {
            steps {
                git 'https://github.com/harshadaborse26/netresh-.git'
                echo 'Pull Success'
            }
        }
}
}
